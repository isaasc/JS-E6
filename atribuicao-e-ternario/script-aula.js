const numero = 20;
numero += 10; // numero = numero + 10;
numero *= 10; // numero = numero * 10;

const idade = 20;
const podeBeber = (idade >= 18) ? 'Pode beber' : 'Não pode beber';
// condição ? true : false
